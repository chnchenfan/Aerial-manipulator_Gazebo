<launch>
  <arg name="namespace" default="uav_arm"/>

  <!-- Load PID gains into the controller namespace -->
  <rosparam file="$(find arm_controller)/config/joint_pid_uam_v5.yaml"
            command="load"
            ns="$(arg namespace)"/>

  <!-- Spawn controllers once Gazebo publishes the ros_control hardware -->
  <node pkg="controller_manager"
        type="spawner"
        name="uam_v5_controller_spawner"
        args="joint_state_controller
              arm_joint1_position_controller
              arm_joint2_position_controller
              left_hand_joint_position_controller"
        ns="$(arg namespace)"
        output="screen"/>
</launch>
